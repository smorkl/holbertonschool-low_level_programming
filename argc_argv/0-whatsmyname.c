#include <stdio.h>

/**
 * main - the fuction print the name of touch.
 * @argc: the number of arguments
 * @argv: an array of pointers to the arguments.
 *
 * Return: 0
 */

int main(int argc, char *argv[])
{
	(void)argc;
	printf("%s\n", argv[0]);

	return (0);
}
