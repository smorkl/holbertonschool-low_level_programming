#include "main.h"

/**
 * _abs - Calculates the absolute value of an integer.
 * @num: number
 *
 * Return: - if is negative o + if is postive
 */

int _abs(int num)
{
	if (num < 0)
	{
		return (-num);
	}
	else
	{
		return (num);
	}
}
