#include "main.h"

/**
 *
 *
 *
 */

void print_array(int *a, int n);
{
	int contador = 0;

	while (contador < n)
	{
		if (contador < (n - 1))
		{
			printf("%d, " a[contador]);
			contador++;
		}
		else
		{
			printf("%d" a[contador]);
			contador++;
		}
	}
	_putchar("\n");
}
